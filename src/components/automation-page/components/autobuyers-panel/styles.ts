import { css } from 'lit';

const styles = css`
    :host {
      width: 100%;
      display: flex;
      align-items: stretch;
      flex-direction: column;
      gap: var(--sl-spacing-large);
    }
  `;

  export default styles;